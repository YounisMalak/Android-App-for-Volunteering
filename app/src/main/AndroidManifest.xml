<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.newprojectmishanxx">

    <application
        android:allowBackup="true"
        android:icon="@drawable/mishan_eng"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewProjectMishanXx">
        <activity
            android:name=".Assign_Volunteer_To_Resident_activity"
            android:exported="false" />
        <activity
            android:name=".Drawer_options_activity"
            android:exported="false" />
        <activity
            android:name=".Users_Activity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>